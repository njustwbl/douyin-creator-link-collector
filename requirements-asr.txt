# 本地内容文本化组件。第一次安装体积较大，后续不会重复安装。
# PyTorch/torchaudio 由 bootstrap.py 根据硬件单独安装，禁止在本文件中重新声明，
# 否则 pip 可能把 CUDA 版覆盖成 CPU 版。
pdm-backend>=2.4,<3.0
funasr>=1.3.3,<2.0
modelscope>=1.20,<2.0
soundfile>=0.12,<1.0
imageio-ffmpeg>=0.6,<1.0

# Whisper 专用高性能本地推理。
faster-whisper>=1.1,<2.0

# 图文 OCR：默认中文/英文，CPU ONNX Runtime 本地运行。
rapidocr>=3.0,<4.0
onnxruntime>=1.18,<2.0

