[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
  ".git",
  ".venv",
  "data",
  "docs",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
